{
  "name": "knot-escape",
  "version": "1.0.0",
  "description": "Puzzle Park — a collection of mini puzzle games",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.js",
    "cap:sync": "npm run build && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
